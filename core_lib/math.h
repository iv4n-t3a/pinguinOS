#ifndef CORE_LIB_MATH_H
#define CORE_LIB_MATH_H

#define MAX(A, B) ((A) > (B) ? (A) : (B))
#define MIN(A, B) ((A) < (B) ? (A) : (B))
#define BETWEEN(X, A, B) ((A) <= (X) && (X) <= (B))

#endif // #ifndef CORE_LIB_MATH_H
